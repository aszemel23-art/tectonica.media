{
  "name": "tectonica-media",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/check.mjs && node scripts/check-media.mjs && node scripts/check-editorial.mjs && node scripts/test-publication-guards.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
